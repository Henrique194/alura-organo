export default class Collaborator {
    constructor(course, imageUrl, name, role) {
        this.course = course;
        this.imageUrl = imageUrl;
        this.name = name;
        this.role = role;
    }
}